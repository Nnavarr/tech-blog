<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css'>
</head>
<body>
    <header class="container">
        <nav>
            {{#if loggedIn}}
            <a href="/">
                <button id='home' class="btn btn-secondary">Home</button>
            </a>
            <a href="/dashboard">
                <button id='dashboard' class="col btn btn-secondary">Dashboard</button>
            </a>
            <a href="/logout">
                <button id='logout' class="col btn btn-secondary">Logout</button>
            </a>
            {{else}}
            {{!-- if the user is not logged in --}}
            <a href="/">
                <button id='home' class="btn btn-secondary">Home</button>
            </a>
            <a href="/login">
                <button id='dashboard' class="col btn btn-secondary">Dashboard</button>
            </a>
            <a href="/login">
                <button id='login' class="col btn btn-secondary">Login</button>
            </a>
            {{/if}}
        </nav>
    </header>
    <h1 class="main-title container">
        Tech Blog Spot
    </h1>
    <main>
        {{{ body }}}
    </main>
    <footer>
        By Noe Navarro 2022
    </footer>

    {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>